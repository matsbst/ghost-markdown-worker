{
  "name": "ghost-markdown-worker",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node scripts/vendor-turndown.mjs --check && node --test",
    "build": "wrangler deploy --dry-run",
    "vendor:turndown": "node scripts/vendor-turndown.mjs"
  },
  "dependencies": {
    "@mixmark-io/domino": "^2.2.0",
    "turndown": "^7.2.4"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "miniflare": "5.20260916.0-alpha",
    "wrangler": "^4.133.0"
  }
}
