{
  "name": "ghost-markdown-worker",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.3.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@mixmark-io/domino": "^2.2.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
